package com.example.project;

public class Trophy extends Treasure { //child of treasure
    // constructs a Trophy object using the super constructor, inheriting from Treasure
    public Trophy(int x, int y) {
        super(x, y);
    }
}